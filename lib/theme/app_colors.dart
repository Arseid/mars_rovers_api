import 'package:flutter/material.dart';

class AppColors {
  static const background = Colors.white;
  static const cardBackground = Color(0xFFDECECA);
  static const drawerBackground = Color(0xFFFAF0F2);

  static const primaryText = Color(0xFF000000);
  static const secondaryText = Color(0xFF607D8B);
  static const accentText = Colors.red;

  static const buttonBackground = Color(0xFFFAF0F1);
  static const buttonText = Color(0xFF963700);

  static const divider = Color(0xFFDBC6C0);

  static const error = Colors.red;
  static const overlay = Colors.black;
  static const overlayText = Colors.white;

  static final appBarColor = Colors.deepOrange[200];
}
